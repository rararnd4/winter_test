
  # Disaster Response App UI

  This is a code bundle for Disaster Response App UI. The original project is available at https://www.figma.com/design/B8HAIqc6p4OKQyG3rzSKrg/Disaster-Response-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  